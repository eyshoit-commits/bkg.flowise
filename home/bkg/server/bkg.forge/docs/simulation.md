# SIMStudio

SIMStudio wird als externer Dienst angenommen. Die API-Route `/api/sim` leitet GET/POST an `SIMService` weiter, der auf `SIMSTUDIO_URL` zeigt. Simulationsergebnisse werden im WebUI im Tab „Simulation“ gezeigt.
