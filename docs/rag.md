# RAG

Die RAG-Pipeline nutzt Chunking (Markdown/Code/Text), Embeddings via LMStudio/LlamaEdge und pgvector-Suche. Die Indexierung wird durch `indexChunks` umgesetzt, Retrieval über cosine Similarity.
