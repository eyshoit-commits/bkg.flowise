# Simulation

- `SIMService` sendet JSON an SIMStudio (`BKG_FORGE_SIM_URL`).
- API Endpoint: `/api/sim` leitet durch und liefert JSON-Ergebnis.
- WebUI bietet Formular für Szenario und Parameter-JSON.
